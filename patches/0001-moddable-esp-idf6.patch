diff --git a/components/microlink/components/wireguard_lwip/src/wireguardif.c b/components/microlink/components/wireguard_lwip/src/wireguardif.c
index 6627fd0..b947e47 100644
--- a/components/microlink/components/wireguard_lwip/src/wireguardif.c
+++ b/components/microlink/components/wireguard_lwip/src/wireguardif.c
@@ -539,9 +539,10 @@ static void wireguardif_process_data_message(struct wireguard_device *device, st
 								if (dest_ok) {
 									// Send packet to be processed by LWIP
 									WG_DEBUG("[WG_RX_IP] Passing %u bytes to IP layer\n", (unsigned)pbuf->tot_len);
-									ip_input(pbuf, device->netif);
-									// pbuf is owned by IP layer now
-									pbuf = NULL;
+									if (device->netif->input(pbuf, device->netif) == ERR_OK) {
+										// pbuf is owned by IP layer now
+										pbuf = NULL;
+									}
 								} else {
 									WG_DEBUG("[WG_RX_IP] DROPPED: dest_ok=false\n");
 								}
@@ -755,8 +756,9 @@ void wireguardif_network_rx(void *arg, struct udp_pcb *pcb, struct pbuf *p, cons
 
 	uint8_t type = wireguard_get_message_type(data, len);
 
-	// Always log incoming WG packets (critical for debugging handshake issues)
-	printf("[WG_RX] type=%d (%s) len=%u from %s:%u\n",
+	// Packet-level DATA logging can fill the USB serial buffer and block the app.
+	// Handshake diagnostics below remain enabled; full packet logs are debug-only.
+	WG_DEBUG("[WG_RX] type=%d (%s) len=%u from %s:%u\n",
 		type,
 		type == 1 ? "INIT" : type == 2 ? "RESP" : type == 3 ? "COOKIE" : type == 4 ? "DATA" : "?",
 		(unsigned)len,
diff --git a/components/microlink/include/microlink_internal.h b/components/microlink/include/microlink_internal.h
index add69c7..1c68b17 100644
--- a/components/microlink/include/microlink_internal.h
+++ b/components/microlink/include/microlink_internal.h
@@ -16,17 +16,16 @@
 #pragma once
 
 #include "microlink.h"
-#include "ml_config_httpd.h"
-#include "sdkconfig.h"
+#include "ml_config_httpd.h"
+#include "sdkconfig.h"
+#include "microlink_moddable_config.h"
 #include "freertos/FreeRTOS.h"
 #include "freertos/task.h"
 #include "freertos/queue.h"
 #include "freertos/event_groups.h"
-#include "freertos/semphr.h"
-#include "mbedtls/ssl.h"
-#include "mbedtls/entropy.h"
-#include "mbedtls/ctr_drbg.h"
-#include "esp_heap_caps.h"
+#include "freertos/semphr.h"
+#include "mbedtls/ssl.h"
+#include "esp_heap_caps.h"
 
 #ifdef CONFIG_ML_ZERO_COPY_WG
 #include "lwip/udp.h"
@@ -321,12 +320,12 @@ typedef struct {
  * ========================================================================== */
 
 typedef struct {
-    int sockfd;                     /* Raw TCP socket */
-    mbedtls_ssl_context ssl;        /* TLS context (owned exclusively by DERP I/O task) */
-    mbedtls_ssl_config ssl_conf;
-    mbedtls_entropy_context entropy;
-    mbedtls_ctr_drbg_context ctr_drbg;
-    bool connected;
+    int sockfd;                     /* Raw TCP socket */
+    mbedtls_ssl_context ssl;        /* TLS context (owned exclusively by DERP I/O task) */
+    mbedtls_ssl_config ssl_conf;
+    bool tls_initialized;
+    bool crt_bundle_attached;
+    bool connected;
     uint64_t last_recv_ms;          /* For keepalive watchdog */
 } ml_derp_conn_t;
 
diff --git a/components/microlink/src/ml_derp.c b/components/microlink/src/ml_derp.c
index c57fd0f..fd04b03 100644
--- a/components/microlink/src/ml_derp.c
+++ b/components/microlink/src/ml_derp.c
@@ -21,21 +21,47 @@
 
 #include "microlink_internal.h"
 #include "esp_log.h"
-#include "esp_timer.h"
-#include "esp_random.h"
+#include "esp_timer.h"
+#include "esp_random.h"
+#include "esp_crt_bundle.h"
 #include "lwip/sockets.h"
 #include "lwip/netdb.h"
-#include "mbedtls/net_sockets.h"
-#include "mbedtls/error.h"
+#include "mbedtls/net_sockets.h"
+#include "mbedtls/error.h"
+#include "psa/crypto.h"
 #include "nacl_box.h"
 #include <string.h>
 #include <errno.h>
 #include <fcntl.h>
 
-static const char *TAG = "ml_derp";
+static const char *TAG = "ml_derp";
 
 /* Timeout for DERP connection handshake operations */
-#define DERP_CONNECT_TIMEOUT_MS  10000
+#define DERP_CONNECT_TIMEOUT_MS  10000
+
+static void derp_tls_cleanup(microlink_t *ml, bool send_close_notify) {
+    if (ml->derp.tls_initialized) {
+        if (send_close_notify && ml->derp.sockfd >= 0) {
+            mbedtls_ssl_close_notify(&ml->derp.ssl);
+        }
+        if (ml->derp.crt_bundle_attached) {
+            esp_crt_bundle_detach(&ml->derp.ssl_conf);
+            ml->derp.crt_bundle_attached = false;
+        }
+        mbedtls_ssl_free(&ml->derp.ssl);
+        mbedtls_ssl_config_free(&ml->derp.ssl_conf);
+        ml->derp.tls_initialized = false;
+    }
+    if (ml->derp.sockfd >= 0) {
+        ml_close_sock(ml->derp.sockfd);
+        ml->derp.sockfd = -1;
+    }
+}
+
+static esp_err_t derp_tls_fail(microlink_t *ml) {
+    derp_tls_cleanup(ml, false);
+    return ESP_FAIL;
+}
 
 /* ============================================================================
  * Custom BIO callbacks for non-blocking TLS I/O
@@ -708,46 +734,66 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
     int64_t t_derp_tcp = esp_timer_get_time();
     ESP_LOGI(TAG, "[TIMING] DERP TCP connect: %lld ms", (t_derp_tcp - t_derp_dns) / 1000);
 
-    /* TLS setup */
-    mbedtls_ssl_init(&ml->derp.ssl);
-    mbedtls_ssl_config_init(&ml->derp.ssl_conf);
-    mbedtls_entropy_init(&ml->derp.entropy);
-    mbedtls_ctr_drbg_init(&ml->derp.ctr_drbg);
-
-    mbedtls_ctr_drbg_seed(&ml->derp.ctr_drbg, mbedtls_entropy_func,
-                           &ml->derp.entropy, NULL, 0);
-
-    mbedtls_ssl_config_defaults(&ml->derp.ssl_conf,
-                                 MBEDTLS_SSL_IS_CLIENT,
-                                 MBEDTLS_SSL_TRANSPORT_STREAM,
-                                 MBEDTLS_SSL_PRESET_DEFAULT);
-    mbedtls_ssl_conf_authmode(&ml->derp.ssl_conf, MBEDTLS_SSL_VERIFY_NONE);
-    mbedtls_ssl_conf_rng(&ml->derp.ssl_conf, mbedtls_ctr_drbg_random, &ml->derp.ctr_drbg);
-    mbedtls_ssl_conf_read_timeout(&ml->derp.ssl_conf, DERP_CONNECT_TIMEOUT_MS);
-
-    mbedtls_ssl_setup(&ml->derp.ssl, &ml->derp.ssl_conf);
-    mbedtls_ssl_set_hostname(&ml->derp.ssl, derp_host);
+    /* TLS setup */
+    ml->derp.sockfd = sock;
+    mbedtls_ssl_init(&ml->derp.ssl);
+    mbedtls_ssl_config_init(&ml->derp.ssl_conf);
+    ml->derp.tls_initialized = true;
+
+    psa_status_t psa_status = psa_crypto_init();
+    if (psa_status != PSA_SUCCESS) {
+        ESP_LOGE(TAG, "PSA Crypto init failed: %ld", (long)psa_status);
+        return derp_tls_fail(ml);
+    }
+
+    int ret = mbedtls_ssl_config_defaults(&ml->derp.ssl_conf,
+                                      MBEDTLS_SSL_IS_CLIENT,
+                                      MBEDTLS_SSL_TRANSPORT_STREAM,
+                                      MBEDTLS_SSL_PRESET_DEFAULT);
+    if (ret != 0) {
+        ESP_LOGE(TAG, "TLS config failed: -0x%04x", -ret);
+        return derp_tls_fail(ml);
+    }
+    if (esp_crt_bundle_attach(&ml->derp.ssl_conf) != ESP_OK) {
+        ESP_LOGE(TAG, "TLS certificate bundle attach failed");
+        return derp_tls_fail(ml);
+    }
+    ml->derp.crt_bundle_attached = true;
+    mbedtls_ssl_conf_authmode(&ml->derp.ssl_conf, MBEDTLS_SSL_VERIFY_REQUIRED);
+    mbedtls_ssl_conf_read_timeout(&ml->derp.ssl_conf, DERP_CONNECT_TIMEOUT_MS);
+
+    ret = mbedtls_ssl_setup(&ml->derp.ssl, &ml->derp.ssl_conf);
+    if (ret != 0) {
+        ESP_LOGE(TAG, "TLS setup failed: -0x%04x", -ret);
+        return derp_tls_fail(ml);
+    }
+    ret = mbedtls_ssl_set_hostname(&ml->derp.ssl, derp_host);
+    if (ret != 0) {
+        ESP_LOGE(TAG, "TLS hostname setup failed: -0x%04x", -ret);
+        return derp_tls_fail(ml);
+    }
     /* Store socket fd BEFORE setting bio.
      * Use custom BIO callbacks that route through ml_read_sock/ml_write_sock,
      * which transparently support both lwIP and AT socket backends.
      * Timeout is handled via SO_RCVTIMEO. */
-    ml->derp.sockfd = sock;
-    mbedtls_ssl_set_bio(&ml->derp.ssl, &ml->derp.sockfd,
-                         ml_derp_bio_send, NULL, ml_derp_bio_recv_timeout);
-
-    /* TLS handshake - socket has 10s SO_RCVTIMEO from connect phase. */
-    int ret;
-    while ((ret = mbedtls_ssl_handshake(&ml->derp.ssl)) != 0) {
+    mbedtls_ssl_set_bio(&ml->derp.ssl, &ml->derp.sockfd,
+                         ml_derp_bio_send, NULL, ml_derp_bio_recv_timeout);
+
+    /* TLS handshake - socket has 10s SO_RCVTIMEO from connect phase. */
+    while ((ret = mbedtls_ssl_handshake(&ml->derp.ssl)) != 0) {
         if (ret == MBEDTLS_ERR_SSL_WANT_READ || ret == MBEDTLS_ERR_SSL_WANT_WRITE) {
             continue;
         }
         char err_buf[128];
         mbedtls_strerror(ret, err_buf, sizeof(err_buf));
         ESP_LOGE(TAG, "TLS handshake failed: %s", err_buf);
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
-    }
+        return derp_tls_fail(ml);
+    }
+
+    if (mbedtls_ssl_get_verify_result(&ml->derp.ssl) != 0) {
+        ESP_LOGE(TAG, "DERP TLS certificate verification failed");
+        return derp_tls_fail(ml);
+    }
 
     int64_t t_derp_tls = esp_timer_get_time();
     ESP_LOGI(TAG, "[TIMING] DERP TLS handshake: %lld ms", (t_derp_tls - t_derp_tcp) / 1000);
@@ -764,11 +810,9 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
              derp_host);
 
     ret = mbedtls_ssl_write(&ml->derp.ssl, (const uint8_t *)upgrade_req, strlen(upgrade_req));
-    if (ret < 0) {
-        ESP_LOGE(TAG, "Failed to send HTTP upgrade");
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+    if (ret < 0) {
+        ESP_LOGE(TAG, "Failed to send HTTP upgrade");
+        return derp_tls_fail(ml);
     }
 
     /* Read HTTP response byte-by-byte until \r\n\r\n to avoid over-reading
@@ -780,11 +824,9 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
         uint64_t http_start = ml_get_time_ms();
 
         while (resp_len < (int)sizeof(resp_buf) - 1) {
-            if (ml_get_time_ms() - http_start > DERP_CONNECT_TIMEOUT_MS) {
-                ESP_LOGE(TAG, "HTTP upgrade response timeout");
-                ml_close_sock(sock);
-                ml->derp.sockfd = -1;
-                return ESP_FAIL;
+            if (ml_get_time_ms() - http_start > DERP_CONNECT_TIMEOUT_MS) {
+                ESP_LOGE(TAG, "HTTP upgrade response timeout");
+                return derp_tls_fail(ml);
             }
 
             ret = mbedtls_ssl_read(&ml->derp.ssl, resp_buf + resp_len, 1);
@@ -793,17 +835,13 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
                     ret == MBEDTLS_ERR_SSL_TIMEOUT) {
                     vTaskDelay(pdMS_TO_TICKS(10));
                     continue;
-                }
-                ESP_LOGE(TAG, "HTTP upgrade read failed: -0x%04x", -ret);
-                ml_close_sock(sock);
-                ml->derp.sockfd = -1;
-                return ESP_FAIL;
+                }
+                ESP_LOGE(TAG, "HTTP upgrade read failed: -0x%04x", -ret);
+                return derp_tls_fail(ml);
             }
-            if (ret == 0) {
-                ESP_LOGE(TAG, "Connection closed during HTTP upgrade");
-                ml_close_sock(sock);
-                ml->derp.sockfd = -1;
-                return ESP_FAIL;
+            if (ret == 0) {
+                ESP_LOGE(TAG, "Connection closed during HTTP upgrade");
+                return derp_tls_fail(ml);
             }
             resp_len++;
 
@@ -818,11 +856,9 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
 
         resp_buf[resp_len] = '\0';
 
-        if (!found_end || strstr((char *)resp_buf, "101") == NULL) {
-            ESP_LOGE(TAG, "DERP upgrade rejected: %.100s", resp_buf);
-            ml_close_sock(sock);
-            ml->derp.sockfd = -1;
-            return ESP_FAIL;
+        if (!found_end || strstr((char *)resp_buf, "101") == NULL) {
+            ESP_LOGE(TAG, "DERP upgrade rejected: %.100s", resp_buf);
+            return derp_tls_fail(ml);
         }
         ESP_LOGI(TAG, "HTTP 101 Switching Protocols received");
     }
@@ -849,48 +885,38 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
     uint8_t frame_type;
     uint32_t frame_len;
     esp_err_t err = derp_recv_frame_header(ml, &frame_type, &frame_len, DERP_CONNECT_TIMEOUT_MS);
-    if (err != ESP_OK) {
-        ESP_LOGE(TAG, "Failed to read ServerKey frame header (err=%d)", err);
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+    if (err != ESP_OK) {
+        ESP_LOGE(TAG, "Failed to read ServerKey frame header (err=%d)", err);
+        return derp_tls_fail(ml);
     }
 
     if (frame_type != DERP_FRAME_SERVER_KEY || frame_len < 40) {
-        ESP_LOGE(TAG, "Expected ServerKey frame (0x01), got 0x%02x len=%lu",
-                 frame_type, (unsigned long)frame_len);
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+        ESP_LOGE(TAG, "Expected ServerKey frame (0x01), got 0x%02x len=%lu",
+                 frame_type, (unsigned long)frame_len);
+        return derp_tls_fail(ml);
     }
 
     /* Read and verify 8-byte magic */
     uint8_t magic[8];
     static const uint8_t DERP_MAGIC[8] = {0x44, 0x45, 0x52, 0x50, 0xf0, 0x9f, 0x94, 0x91};
-    if (derp_tls_read_all(ml, magic, 8, DERP_CONNECT_TIMEOUT_MS) < 0) {
-        ESP_LOGE(TAG, "Failed to read ServerKey magic");
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+    if (derp_tls_read_all(ml, magic, 8, DERP_CONNECT_TIMEOUT_MS) < 0) {
+        ESP_LOGE(TAG, "Failed to read ServerKey magic");
+        return derp_tls_fail(ml);
     }
 
     if (memcmp(magic, DERP_MAGIC, 8) != 0) {
         ESP_LOGE(TAG, "Invalid DERP magic: %02x%02x%02x%02x%02x%02x%02x%02x",
-                 magic[0], magic[1], magic[2], magic[3],
-                 magic[4], magic[5], magic[6], magic[7]);
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+                 magic[0], magic[1], magic[2], magic[3],
+                 magic[4], magic[5], magic[6], magic[7]);
+        return derp_tls_fail(ml);
     }
     ESP_LOGI(TAG, "DERP magic verified");
 
     /* Read 32-byte server public key */
     uint8_t derp_server_key[32];
-    if (derp_tls_read_all(ml, derp_server_key, 32, DERP_CONNECT_TIMEOUT_MS) < 0) {
-        ESP_LOGE(TAG, "Failed to read server key");
-        ml_close_sock(sock);
-        ml->derp.sockfd = -1;
-        return ESP_FAIL;
+    if (derp_tls_read_all(ml, derp_server_key, 32, DERP_CONNECT_TIMEOUT_MS) < 0) {
+        ESP_LOGE(TAG, "Failed to read server key");
+        return derp_tls_fail(ml);
     }
 
     ESP_LOGI(TAG, "DERP server key received (first 8): %02x%02x%02x%02x%02x%02x%02x%02x",
@@ -920,11 +946,9 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
 
         /* Encrypt JSON with NaCl box: our WG private key -> DERP server public key */
         size_t ciphertext_len = json_len + NACL_BOX_MACBYTES;
-        uint8_t *ciphertext = malloc(ciphertext_len);
-        if (!ciphertext) {
-            ml_close_sock(sock);
-            ml->derp.sockfd = -1;
-            return ESP_FAIL;
+        uint8_t *ciphertext = malloc(ciphertext_len);
+        if (!ciphertext) {
+            return derp_tls_fail(ml);
         }
 
         if (nacl_box(ciphertext,
@@ -933,21 +957,17 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
                      derp_server_key,       /* recipient: DERP server */
                      ml->wg_private_key     /* sender: our WG node key */
                      ) != 0) {
-            ESP_LOGE(TAG, "NaCl box encrypt failed");
-            free(ciphertext);
-            ml_close_sock(sock);
-            ml->derp.sockfd = -1;
-            return ESP_FAIL;
+            ESP_LOGE(TAG, "NaCl box encrypt failed");
+            free(ciphertext);
+            return derp_tls_fail(ml);
         }
 
         /* Build ClientInfo frame payload: nodekey(32) + nonce(24) + ciphertext */
         size_t ci_payload_len = 32 + NACL_BOX_NONCEBYTES + ciphertext_len;
         uint8_t *ci_payload = malloc(ci_payload_len);
-        if (!ci_payload) {
-            free(ciphertext);
-            ml_close_sock(sock);
-            ml->derp.sockfd = -1;
-            return ESP_FAIL;
+        if (!ci_payload) {
+            free(ciphertext);
+            return derp_tls_fail(ml);
         }
 
         memcpy(ci_payload, ml->wg_public_key, 32);
@@ -963,11 +983,9 @@ esp_err_t ml_derp_connect(microlink_t *ml) {
 
         /* Send ClientInfo frame */
         if (derp_write_frame(ml, DERP_FRAME_CLIENT_INFO, ci_payload, ci_payload_len) < 0) {
-            ESP_LOGE(TAG, "Failed to send ClientInfo");
-            free(ci_payload);
-            ml_close_sock(sock);
-            ml->derp.sockfd = -1;
-            return ESP_FAIL;
+            ESP_LOGE(TAG, "Failed to send ClientInfo");
+            free(ci_payload);
+            return derp_tls_fail(ml);
         }
         free(ci_payload);
 
@@ -1025,15 +1043,7 @@ void ml_derp_disconnect(microlink_t *ml) {
     ml->derp.connected = false;
     xEventGroupClearBits(ml->events, ML_EVT_DERP_CONNECTED);
 
-    if (ml->derp.sockfd >= 0) {
-        mbedtls_ssl_close_notify(&ml->derp.ssl);
-        mbedtls_ssl_free(&ml->derp.ssl);
-        mbedtls_ssl_config_free(&ml->derp.ssl_conf);
-        mbedtls_ctr_drbg_free(&ml->derp.ctr_drbg);
-        mbedtls_entropy_free(&ml->derp.entropy);
-        ml_close_sock(ml->derp.sockfd);
-        ml->derp.sockfd = -1;
-    }
+    derp_tls_cleanup(ml, true);
 
     /* Drain TX queue */
     ml_derp_tx_item_t item;
diff --git a/components/microlink/src/ml_noise.c b/components/microlink/src/ml_noise.c
index 1b0fac2..aa8fd82 100644
--- a/components/microlink/src/ml_noise.c
+++ b/components/microlink/src/ml_noise.c
@@ -17,8 +17,7 @@
 #include "microlink_internal.h"
 #include "esp_log.h"
 #include "esp_random.h"
-#include "mbedtls/chacha20.h"
-#include "mbedtls/chachapoly.h"
+#include "chacha20poly1305.h"
 #include <string.h>
 
 static const char *TAG = "ml_noise";
@@ -149,68 +148,24 @@ static void noise_mix_key(uint8_t ck[32], uint8_t k[32], const uint8_t *dh_outpu
  * ChaCha20-Poly1305 AEAD (Noise uses 64-bit nonce, padded to 96-bit)
  * ========================================================================== */
 
-static int chacha20poly1305_encrypt(const uint8_t *key, uint64_t nonce,
-                                      const uint8_t *ad, size_t ad_len,
-                                      const uint8_t *plaintext, size_t pt_len,
-                                      uint8_t *ciphertext) {
-    /* Tailscale nonce: 4 bytes zeros + 8 bytes BIG-endian counter
-     * Matches Go: binary.BigEndian.PutUint64(n[4:], counter)
-     * V1 uses __builtin_bswap64 for the same effect */
-    uint8_t nonce_bytes[12];
-    memset(nonce_bytes, 0, 4);
-    nonce_bytes[4]  = (nonce >> 56) & 0xFF;
-    nonce_bytes[5]  = (nonce >> 48) & 0xFF;
-    nonce_bytes[6]  = (nonce >> 40) & 0xFF;
-    nonce_bytes[7]  = (nonce >> 32) & 0xFF;
-    nonce_bytes[8]  = (nonce >> 24) & 0xFF;
-    nonce_bytes[9]  = (nonce >> 16) & 0xFF;
-    nonce_bytes[10] = (nonce >> 8) & 0xFF;
-    nonce_bytes[11] = nonce & 0xFF;
-
-    mbedtls_chachapoly_context ctx;
-    mbedtls_chachapoly_init(&ctx);
-    mbedtls_chachapoly_setkey(&ctx, key);
-
-    /* ciphertext layout: encrypted_data(pt_len) + tag(16) */
-    int ret = mbedtls_chachapoly_encrypt_and_tag(&ctx,
-                pt_len, nonce_bytes, ad, ad_len,
-                plaintext, ciphertext, ciphertext + pt_len);
-
-    mbedtls_chachapoly_free(&ctx);
-    return ret;
-}
-
-static int chacha20poly1305_decrypt(const uint8_t *key, uint64_t nonce,
-                                      const uint8_t *ad, size_t ad_len,
-                                      const uint8_t *ciphertext, size_t ct_len,
-                                      uint8_t *plaintext) {
-    if (ct_len < 16) return -1;
-
-    /* Tailscale nonce: 4 bytes zeros + 8 bytes BIG-endian counter */
-    uint8_t nonce_bytes[12];
-    memset(nonce_bytes, 0, 4);
-    nonce_bytes[4]  = (nonce >> 56) & 0xFF;
-    nonce_bytes[5]  = (nonce >> 48) & 0xFF;
-    nonce_bytes[6]  = (nonce >> 40) & 0xFF;
-    nonce_bytes[7]  = (nonce >> 32) & 0xFF;
-    nonce_bytes[8]  = (nonce >> 24) & 0xFF;
-    nonce_bytes[9]  = (nonce >> 16) & 0xFF;
-    nonce_bytes[10] = (nonce >> 8) & 0xFF;
-    nonce_bytes[11] = nonce & 0xFF;
-
-    size_t payload_len = ct_len - 16;
-    const uint8_t *tag = ciphertext + payload_len;
-
-    mbedtls_chachapoly_context ctx;
-    mbedtls_chachapoly_init(&ctx);
-    mbedtls_chachapoly_setkey(&ctx, key);
-
-    int ret = mbedtls_chachapoly_auth_decrypt(&ctx,
-                payload_len, nonce_bytes, ad, ad_len,
-                tag, ciphertext, plaintext);
-
-    mbedtls_chachapoly_free(&ctx);
-    return ret;
+static int noise_aead_encrypt(const uint8_t *key, uint64_t nonce,
+                              const uint8_t *ad, size_t ad_len,
+                              const uint8_t *plaintext, size_t pt_len,
+                              uint8_t *ciphertext) {
+    /* The bundled implementation encodes its numeric nonce little-endian.
+     * Noise/Tailscale uses the same 32 zero bits followed by a big-endian u64. */
+    chacha20poly1305_encrypt(ciphertext, plaintext, pt_len, ad, ad_len,
+                             __builtin_bswap64(nonce), key);
+    return 0;
+}
+
+static int noise_aead_decrypt(const uint8_t *key, uint64_t nonce,
+                              const uint8_t *ad, size_t ad_len,
+                              const uint8_t *ciphertext, size_t ct_len,
+                              uint8_t *plaintext) {
+    if (ct_len < 16) return -1;
+    return chacha20poly1305_decrypt(plaintext, ciphertext, ct_len, ad, ad_len,
+                                    __builtin_bswap64(nonce), key) ? 0 : -1;
 }
 
 /* ============================================================================
@@ -292,7 +247,7 @@ esp_err_t ml_noise_write_msg1(ml_noise_state_t *state, uint8_t *out, size_t *out
     memset(dh_output, 0, 32);
 
     /* s: Encrypt and send static public key (32 + 16 MAC = 48 bytes) */
-    chacha20poly1305_encrypt(k, 0, state->h, 32,
+    noise_aead_encrypt(k, 0, state->h, 32,
                               state->local_static_public, 32,
                               out + pos);
     noise_mix_hash(state->h, out + pos, 48);
@@ -304,7 +259,7 @@ esp_err_t ml_noise_write_msg1(ml_noise_state_t *state, uint8_t *out, size_t *out
     memset(dh_output, 0, 32);
 
     /* Empty payload encrypted (just the 16-byte auth tag) */
-    chacha20poly1305_encrypt(k, 0, state->h, 32,
+    noise_aead_encrypt(k, 0, state->h, 32,
                               NULL, 0, out + pos);
     noise_mix_hash(state->h, out + pos, 16);
     pos += 16;
@@ -351,7 +306,7 @@ esp_err_t ml_noise_read_msg2(ml_noise_state_t *state, const uint8_t *msg, size_t
 
     /* Decrypt payload (should be empty for IK — just 16-byte auth tag) */
     size_t ciphertext_len = len - offset;
-    if (chacha20poly1305_decrypt(k, 0, state->h, 32,
+    if (noise_aead_decrypt(k, 0, state->h, 32,
                                   msg + offset, ciphertext_len,
                                   NULL) != 0) {
         ESP_LOGE(TAG, "Noise msg2 auth tag verification failed");
@@ -379,7 +334,7 @@ esp_err_t ml_noise_encrypt(const uint8_t *key, uint64_t nonce,
                             const uint8_t *ad, size_t ad_len,
                             const uint8_t *plaintext, size_t pt_len,
                             uint8_t *ciphertext) {
-    if (chacha20poly1305_encrypt(key, nonce, ad, ad_len,
+    if (noise_aead_encrypt(key, nonce, ad, ad_len,
                                   plaintext, pt_len, ciphertext) != 0) {
         ESP_LOGE(TAG, "Noise encrypt failed");
         return ESP_FAIL;
@@ -391,7 +346,7 @@ esp_err_t ml_noise_decrypt(const uint8_t *key, uint64_t nonce,
                             const uint8_t *ad, size_t ad_len,
                             const uint8_t *ciphertext, size_t ct_len,
                             uint8_t *plaintext) {
-    if (chacha20poly1305_decrypt(key, nonce, ad, ad_len,
+    if (noise_aead_decrypt(key, nonce, ad, ad_len,
                                   ciphertext, ct_len, plaintext) != 0) {
         ESP_LOGE(TAG, "Noise decrypt failed (nonce=%llu)", (unsigned long long)nonce);
         return ESP_ERR_INVALID_MAC;
diff --git a/components/microlink/components/wireguard_lwip/src/wireguard-platform-esp32.c b/components/microlink/components/wireguard_lwip/src/wireguard-platform-esp32.c
index f974c67..f0feaf0 100644
--- a/components/microlink/components/wireguard_lwip/src/wireguard-platform-esp32.c
+++ b/components/microlink/components/wireguard_lwip/src/wireguard-platform-esp32.c
@@ -5,9 +5,9 @@
 
 #include "wireguard-platform.h"
 #include "esp_random.h"
-#include "esp_timer.h"
 #include "lwip/sys.h"
 #include <string.h>
+#include <sys/time.h>
 
 /* ============================================================================
  * Time Functions
@@ -20,17 +20,12 @@ uint32_t wireguard_sys_now() {
 
 void wireguard_tai64n_now(uint8_t *output) {
     // TAI64N format: 8 bytes seconds + 4 bytes nanoseconds
-    // For simplicity, use Unix epoch time
-    uint64_t now_us = esp_timer_get_time();
-    uint64_t seconds = now_us / 1000000ULL;
-    uint32_t nanoseconds = (now_us % 1000000ULL) * 1000;
-
-    // Log raw uptime before TAI offset (only every ~5s to avoid spam)
-    static uint64_t last_log_s = 0;
-    if (seconds - last_log_s >= 5) {
-        printf("[TAI64N] uptime=%llu s, nano=%lu\n", (unsigned long long)seconds, (unsigned long)nanoseconds);
-        last_log_s = seconds;
-    }
+    // WireGuard rejects replayed handshake timestamps. ESP timer uptime resets
+    // on every boot, so use the SNTP-synchronized wall clock instead.
+    struct timeval now;
+    gettimeofday(&now, NULL);
+    uint64_t seconds = (uint64_t)now.tv_sec;
+    uint32_t nanoseconds = (uint32_t)now.tv_usec * 1000;
 
     // TAI64 starts at 1970-01-01 00:00:10 TAI (Unix epoch + 10 seconds)
     // Add TAI offset: 2^62 + Unix time
diff --git a/components/microlink/src/ml_wg_mgr.c b/components/microlink/src/ml_wg_mgr.c
index ed40942..bda8fb2 100644
--- a/components/microlink/src/ml_wg_mgr.c
+++ b/components/microlink/src/ml_wg_mgr.c
@@ -186,9 +186,9 @@ static err_t wg_udp_output_cb(uint32_t dest_ip, uint16_t dest_port,
     microlink_t *ml = (microlink_t *)ctx;
     if (!ml) return ERR_CONN;
 
-    /* Log WG packets sent via direct UDP */
+    /* Per-packet logs are debug-only: INFO output can block when USB is unread. */
     uint32_t ip_host = ntohl(dest_ip);
-    ESP_LOGI(TAG, "WG UDP TX: %d bytes -> %d.%d.%d.%d:%d type=%d",
+    ESP_LOGD(TAG, "WG UDP TX: %d bytes -> %d.%d.%d.%d:%d type=%d",
              (int)len,
              (int)((ip_host >> 24) & 0xFF), (int)((ip_host >> 16) & 0xFF),
              (int)((ip_host >> 8) & 0xFF), (int)(ip_host & 0xFF),
@@ -1145,7 +1145,7 @@ static void process_disco_packet(microlink_t *ml, const ml_rx_packet_t *pkt) {
  * ========================================================================== */
 
 static void process_wg_packet(microlink_t *ml, const ml_rx_packet_t *pkt) {
-    ESP_LOGI(TAG, "WG RX: %d bytes, via_derp=%d, type=%d, from=%02x%02x%02x%02x",
+    ESP_LOGD(TAG, "WG RX: %d bytes, via_derp=%d, type=%d, from=%02x%02x%02x%02x",
              (int)pkt->len, pkt->via_derp,
              pkt->len >= 4 ? pkt->data[0] : -1,
              pkt->src_pubkey[0], pkt->src_pubkey[1], pkt->src_pubkey[2], pkt->src_pubkey[3]);
@@ -1621,7 +1621,7 @@ void ml_wg_mgr_task(void *arg) {
             wireguardif_periodic((struct netif *)ml->wg_netif);
             uint64_t dt = ml_get_time_ms() - t0;
             last_wg_periodic_ms = now;
-            ESP_LOGI(TAG, "wireguardif_periodic: %llu ms", (unsigned long long)dt);
+            ESP_LOGD(TAG, "wireguardif_periodic: %llu ms", (unsigned long long)dt);
         }
 
         /* Periodic DISCO probes (every 1s check) */
@@ -1631,7 +1631,7 @@ void ml_wg_mgr_task(void *arg) {
             disco_periodic_probes(ml);
             uint64_t dt = ml_get_time_ms() - t0;
             last_disco_probe_ms = now;
-            ESP_LOGI(TAG, "disco_periodic_probes: %llu ms", (unsigned long long)dt);
+            ESP_LOGD(TAG, "disco_periodic_probes: %llu ms", (unsigned long long)dt);
         }
 
         /* Yield - 10ms loop rate for minimum packet processing latency.
